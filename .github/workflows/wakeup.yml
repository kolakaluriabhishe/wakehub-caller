name: WakeHub Caller

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - name: Call WakeHub
        run: curl https://friendly-wake.lovable.app/
